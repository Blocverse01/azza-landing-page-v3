/**
 * Azza For Business hero - `/products/for-business`, Figma 412:2436.
 *
 * Exported both named and default: components.md S6 describes every section as
 * a default-exported component, while the rest of the codebase imports by name.
 */
export { BusinessHeroArt, type BusinessHeroArtProps } from "./BusinessHeroArt";
export { HeroBusiness, default } from "./HeroBusiness";
