/**
 * Site-wide constants.
 *
 * Infrastructure values only - no design values live here. Colours, type and
 * spacing come from tokens.json via `pnpm tokens`.
 */
export const SITE = {
  name: "AZZA",
  /** Placeholder. Real copy is transcribed from the design in Phase 2. */
  description: "AZZA website",
} as const;
